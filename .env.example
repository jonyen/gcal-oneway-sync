# Google OAuth Credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# For local auth flows (used during initial setup with npm run auth:source and auth:target)
OAUTH_REDIRECT_URI=http://localhost:3333/oauth/callback

# Calendar Configuration
# Comma-separated list of secondary source calendar IDs (from acts2 account)
# Example: team@group.calendar.google.com,abcd123@group.calendar.google.com
SOURCE_CALENDAR_IDS=

# Target is the primary on the Gmail account
TARGET_CALENDAR_ID=primary

# Sync Configuration
FULL_WINDOW_MONTHS=12

# Authentication Tokens (JSON strings - required for Docker/server deployment)
# After running auth:source and auth:target, copy the JSON content from the token files
# and paste as single-line strings below (escape quotes if needed)
# Example: SOURCE_TOKENS_JSON='{"access_token":"...","refresh_token":"...","scope":"...","token_type":"Bearer","expiry_date":...}'
SOURCE_TOKENS_JSON=
TARGET_TOKENS_JSON=

# Server Configuration (for Docker deployment)
NODE_ENV=production
PORT=8080
